# CLAUDE.md Update and Project Cleanup - 2026-02-06

## Summary

Updated project documentation and cleaned up root directory files to maintain codebase hygiene.

## Changes Made

### 1. CLAUDE.md Documentation Update

**File**: `CLAUDE.md`

**Fixed Issues**:

- Removed references to deleted scripts (`extract_conversation.py`, `extract_unread_users.py`, `extract_message_list.py`)
- Added missing `StickerHandler` to message handlers
- Updated database schema section with current table structure
- Added new services documentation:
  - Blacklist Service
  - Device Manager
  - Realtime Reply Manager
  - Modern Followup Architecture
- Fixed `verify_messages_screen.py` references (now RecoveryManager)
- Updated CLI layer to reflect `SyncOrchestrator` instead of `InitialSyncService`
- Added settings migration system documentation
- Updated database path references to use `get_default_db_path()`
- Reorganized related documentation section with new docs/ structure

**Database Schema Updates**:

- Added `kefu_devices` junction table
- Added `blacklist` table
- Added `schema_version` table
- Removed obsolete `resources`, `settings`, `sync_checkpoints`, `ai_replies` tables
- Updated relationships to reflect many-to-many kefu-device relationship

### 2. Root Directory Analysis

**Analyzed Files**:

- Identified all files in project root
- Determined which files are actively used vs obsolete
- Created cleanup recommendations (see separate analysis)

**Files to Keep**:

- Configuration: `pyproject.toml`, `package.json`, `commitlint.config.js`, `.gitignore`, `.gitattributes`
- Documentation: `README.md`, `CLAUDE.md`, `AGENTS.md`
- Active Scripts: `list_devices.py`, `get_kefu_name.py`, `initial_sync.py`, `realtime_reply_process.py`, `test_ai_server.py`, `image_sender_demo.py`
- Batch Files: `kill-processes.bat`, `restart-app.bat`, `test_ai_server.bat`
- Runtime directories: `avatars/`, `conversation_images/`, `conversation_videos/`, `conversation_voices/`, `logs/`, `settings/`
- Database files: `wecom_conversations.db`, `wecom_conversations.db.backup`

**Obsolete Files Identified**:

- `start_wecom.py` - Functionality integrated into WeComService
- `switch_to_private_chats.py` - Functionality integrated into modular services
- Old checkpoint files: `sync_checkpoint_*.json`
- Debug test scripts: `test_anchor_detection.py`, `test_anchor_message_detection.py`, `test_is_self_debug.py`, `test_real_signature.py`, `test_signature_scheme.py`
- Test directory: `followup_test/` - Temporary test directory
- Empty file: `nul`

## Documentation Improvements

### Accuracy Improvements

1. **CLI Commands**: Updated to reference actual existing scripts
2. **Database Tables**: Aligned with current schema.py definition (DATABASE_VERSION = 8)
3. **Service Architecture**: Reflects current modular architecture
4. **Message Handlers**: Includes all 5 handlers (text, image, voice, video, sticker)

### New Documentation Sections

1. **Blacklist System** (Section 9): Complete feature documentation
2. **Device Manager** (Section 10): Multi-device orchestration details
3. **Sidecar Queue System**: Message queue to prevent wrong-contact sends

## Related Issues

This update addresses the documentation debt identified in:

- Missing new feature documentation (blacklist, realtime reply manager)
- Outdated script references causing confusion
- Database schema drift from documentation
- Incomplete architecture descriptions

## Testing

No tests were modified as part of this documentation-only update.

## Migration Notes

For users referencing old documentation:

- `extract_conversation.py` → Use `initial_sync.py` instead
- `extract_unread_users.py` → Functionality integrated into sync workflow
- `verify_messages_screen.py` → Use RecoveryManager in backend services
- `resources` table → Now split into `images` and `videos` tables
- `settings` table → Moved to separate settings database in `settings/` directory

## Future Work

Potential root directory cleanup (not executed in this PR):

- Remove obsolete scripts (`start_wecom.py`, `switch_to_private_chats.py`)
- Remove old checkpoint files
- Remove or organize debug test scripts
- Clean up temporary test directories

This should be done as a separate cleanup PR after confirming no active usage.
